# Copia este archivo a .env y rellena valores reales.
# NUNCA subas el .env real a git (ya debería estar en .gitignore).

# --- Base de datos ---
DB_DATABASE=paddock_daily
DB_USER=directus
DB_PASSWORD=cambia_esto_por_una_contrasena_fuerte

# --- Directus ---
# Genera uno nuevo con: openssl rand -hex 32
SECRET=cambia_esto_por_un_secreto_aleatorio_largo

ADMIN_EMAIL=froddes.dev@gmail.com
ADMIN_PASSWORD=cambia_esto_por_una_contrasena_fuerte_distinta
